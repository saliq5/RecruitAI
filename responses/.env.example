# RecruitAI - Local Development Environment Variables (example)
# Copy to .env in the same folder before running docker compose:
#   cp .env.example .env

# =========================
# PostgreSQL
# =========================
POSTGRES_DB=recruitai
POSTGRES_USER=recruitai
POSTGRES_PASSWORD=recruitai

# Backend JDBC URL for local docker-compose (points to the postgres container name)
DB_URL=jdbc:postgresql://postgres:5432/recruitai
DB_USER=recruitai
DB_PASSWORD=recruitai

# =========================
# Elasticsearch
# =========================
# Local container URL (inside compose network), frontend should call backend, not ES directly
ELASTIC_URL=http://elasticsearch:9200
# Tweak ES heap for your machine
ES_JAVA_OPTS=-Xms1g -Xmx1g

# Cloud (optional) - if using Elastic Cloud or Bonsai, set these instead of disabling security
ELASTIC_API_KEY=
ES_USER=
ES_PASS=

# =========================
# Security
# =========================
# Change for any non-local environment; rotate periodically
JWT_SECRET=dev-secret-change-me

# =========================
# LLMs and Transcription
# =========================
# Selects which provider adapter to use: local | together | groq | hf | openrouter
LLM_PROVIDER=local
# API key if using a hosted provider (leave empty for local)
LLM_API_KEY=
# Embedding model identifier (for local/hosted embedding)
EMBED_MODEL=e5-small
# Path to local whisper binary (if used by backend)
WHISPER_PATH=/usr/local/bin/whisper

# =========================
# Frontend
# =========================
# React app will call the backend at this base URL
VITE_API_BASE=http://localhost:8080/api

# =========================
# Optional: Override images (if you have your own builds)
# =========================
# BACKEND_IMAGE=ghcr.io/your-org/recruitai-backend:dev
# FRONTEND_IMAGE=ghcr.io/your-org/recruitai-frontend:dev
